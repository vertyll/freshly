spring:
  docker:
    compose:
      enabled: false

  mongodb:
    host: localhost
    port: 27018
    database: test_db
    username: test
    password: test
    authentication-database: admin
    representation:
      uuid: STANDARD

  mail:
    host: localhost
    port: 1025
    username: vertyll
    password: vertyll
    from: no-reply@freshly.local
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

  task:
    scheduling:
      enabled: false

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000/realms/freshly-test
          jwk-set-uri: http://localhost:9000/realms/freshly-test/protocol/openid-connect/certs

security:
  jwt:
    secret: isN5Z1EinzeD2JyjXq6SZqoBd+QpOJMxZsBHwfhXHAA=
    expiration:
      email-verification: 3600000
      password-reset: 1800000

external-service:
  gios:
    api-url: http://localhost:8080/mock-gios-api

application:
  file:
    uploads:
      file-output-path: ./test-uploads

  frontend:
    url: http://localhost:4200

  keycloak:
    server-url: http://localhost:9000
    realm: freshly-test
    admin-client-id: freshly-service-account-test
    admin-client-secret: IEKlEpXPeu0Cd0SjUzEPImaIz8NDougC
    user-client-id: freshly-app-client-test
    user-client-secret: YTJcSSBrtEuC2VoNFlGbNVVMPtqokEC8
    roles-claim-path: realm_access.roles
    cookie:
      refresh-token-cookie-name: KEYCLOAK_REFRESH_TOKEN
      http-only: true
      secure: true
      same-site: Strict
      path: "/"

server:
  port: 0
